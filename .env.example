# Firebase web config is client-side; restrict API key by HTTP referrer/API in Google Cloud.
# Flutterwave public key only; never expose Flutterwave secret key in frontend.
VITE_FLUTTERWAVE_PUBLIC_KEY=

# Kivora owner billing bypass is hard-coded in src/access.js and functions/access.js.
# Do not add the owner email as a client-supplied environment variable.
